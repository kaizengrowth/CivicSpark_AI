# Evidence-layer ingestion: source adapters fetch raw documents from
# City of Tulsa systems; the pipeline parses agenda structure, resolves
# entities, classifies topics, chunks per agenda item, embeds, and
# records provenance + scrape_runs bookkeeping.
