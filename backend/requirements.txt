# Runtime dependencies — kept deliberately small so the API fits comfortably
# on a 512MB Render instance. Dev/test tooling lives in requirements-dev.txt.

# FastAPI and server
fastapi>=0.104.0,<0.110.0
uvicorn[standard]==0.24.0
python-multipart>=0.0.7

# HTTP clients
httpx>=0.25.0,<0.28.0
requests>=2.31.0

# Database (Postgres is the only stateful service: relational data + pgvector RAG index)
sqlalchemy>=2.0.0,<2.1.0
alembic==1.13.0
psycopg2-binary==2.9.9

# RAG: embeddings via OpenAI API, similarity via pgvector or numpy fallback
openai>=1.3.0
tiktoken>=0.5.0
numpy<2.0.0,>=1.24.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0

# Notifications (optional at runtime — features degrade without credentials)
twilio>=8.10.0

# Document processing (pure-Python extractors only)
pypdf>=3.17.0
pdfplumber>=0.10.0
python-docx>=1.1.0

# Web scraping (requests + BeautifulSoup, no browser automation)
beautifulsoup4>=4.12.0
python-dateutil>=2.8.0
pytz>=2023.3

# Utilities
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0
email-validator>=2.1.0

# Optional integrations (install manually if used):
#   boto3                     — S3 storage backend (STORAGE_BACKEND=s3)
#   google-api-python-client  — Google Custom Search for chatbot web research
#   python-magic              — content-based MIME detection (extension fallback otherwise)
