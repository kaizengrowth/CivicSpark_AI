# CivicSpark AI — root environment variables (local docker-compose)
# Backend runtime configuration lives in backend/env.example.

# Port overrides for docker-compose (optional)
POSTGRES_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Passed through to the backend container by docker-compose
OPENAI_API_KEY=your-openai-api-key
